Assignment 0, implementing the Knight's Tour.

The implementation in my code uses recursion and backtracking (as shown on the wikipedia page) to find the tours on a (m x n) board, it should work on arbitrary sizes, but I have not tested on anything other than a 5x5 board.